#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/wpasupplicant" ] || [ -e "/etc/init/wpasupplicant.conf" ]; then
	invoke-rc.d wpasupplicant stop || exit $?
fi
# End automatically added section
